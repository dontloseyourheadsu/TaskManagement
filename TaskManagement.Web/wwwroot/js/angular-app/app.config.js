// Config values for Task Management application
(function () {
  "use strict";

  // Create a global configuration object for the application
  window.taskManagementConfig = {
    // API base URL (change this when deploying to different environments)
    apiBaseUrl: "https://localhost:7001",
  };
})();
